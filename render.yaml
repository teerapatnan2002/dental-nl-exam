services:
  - type: web
    name: dental-nl-exam
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "./start.sh"
    plan: free
    region: singapore
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: JWT_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: "*"
      - key: DATABASE_URL
        value: "sqlite:///./data/exam_bank.db"
